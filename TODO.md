# TODO

## Next product improvements

- Add optional **watched names / aliases** support for digest matching.
  - Examples:
    - `@Tony`
    - `team-edu`
- Make this configurable per user in the settings modal.
- Decide how to present the match reason in the digest:
  - direct mention
  - watched emoji
  - watched name / alias
- Keep alias/name matching opt-in to avoid noisy false positives.

## 다음 개선 사항

- 다이제스트 매칭에 사용할 **감시 이름 / 별칭(alias)** 기능 추가
  - 예시:
    - `@Tony`
    - `team-edu`
- 설정 모달에서 사용자별로 직접 입력 가능하게 만들기
- 다이제스트에서 어떤 이유로 매칭됐는지 표시 방식 결정
  - 직접 멘션
  - 감시 이모지
  - 감시 이름 / alias
- 이름 / alias 매칭은 노이즈를 줄이기 위해 opt-in 방식으로 유지하기
